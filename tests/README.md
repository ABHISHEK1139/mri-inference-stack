# Tests

This folder is intentionally lightweight in the public snapshot.

Current quality gates run through:
- static linting (Ruff)
- compile smoke checks
- project preflight checks

Add unit tests here as dataset-independent modules are expanded.
